{
  "name": "playwright-cucumber-api",
  "version": "1.0.0",
  "description": "Reusable API testing sample with Playwright and Cucumber",
  "scripts": {
    "test": "cucumber-js",
    "test:allure": "cucumber-js && node scripts/cucumber2allure.js && npx allure generate allure-results -o allure-report --clean"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "@playwright/test": "^1.40.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "allure-commandline": "^2.13.8",
    "chai": "^4.3.8",
    "dotenv": "^17.4.2"
  }
}
